//
// Draw Triangles
//
CLS 
//
CYCLE 
  COLOUR = RND (16)
  x1 = RND (gWidth)
  x2 = RND (gWidth)
  x3 = RND (gWidth)
  y1 = RND (gHeight)
  y2 = RND (gHeight)
  y3 = RND (gHeight)
  f = RND (2)
  TRIANGLE (x1, y1, x2, y2, x3, y3, f)
  UPDATE 
  IF INKEY <> -1 THEN BREAK 
REPEAT 
END 
